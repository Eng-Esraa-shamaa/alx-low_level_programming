#include "main.h"
#include<stdio.h>
/**
 *main - the main function to the program
 *@argc: the number of arguments
 *@argv: the vector to arguments
 *Return: the name of the program
 */
int __attribute__ ((unused)) main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
